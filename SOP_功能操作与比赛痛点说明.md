# 工学智链：功能操作、演示与验收 SOP

版本：1.0  
最后验证：2026-08-13
适用对象：普通学习者、知识贡献者、治理管理员、项目演示人员

## 一、产品定位

工学智链不是通用聊天壳，也不是把课程列表换成 AI 推荐。它面向计算机学习与职业发展，把用户从“我不知道选什么方向”带到“我知道为什么选、先学什么、如何练、如何证明会了、下一步如何调整”的完整闭环。

平台当前包含：

- 3 个计算机方向簇；
- 16 个主方向、29 条细分学习/职业路线；
- 60 个路线技能节点；
- 每条路线的依赖关系、诊断、代表项目和审核来源；
- 6 个协作 Agent；
- 4 类生成资源；
- 6 类冻结学习者画像与 64 条冻结评测任务（16 个主方向各 4 条）。

主方向包括 Web 前端、后端、全栈、移动端、测试与质量、云原生与 DevOps、算法、机器学习、LLM 应用、Agent 全栈、嵌入式与物联网、操作系统与系统编程、网络与安全、数据库系统与内核、数据工程、UI/UX。其下进一步拆为 React、Vue、Java、Python、Go、Node.js、C++/Rust、iOS、Android、Flutter、React Native 等 29 条可直接执行的技术路线。

## 二、启动 SOP

### 2.1 本地开发

后端：

```powershell
cd E:\multi-agent-engi-learning-portrait\backend
.\venv\Scripts\Activate.ps1
python -m alembic upgrade head
python -m uvicorn app.main:app --reload --port 8000
```

首次安装时先执行：

```powershell
python -m venv venv
.\venv\Scripts\Activate.ps1
pip install -r requirements.txt
```

前端：

```powershell
cd E:\multi-agent-engi-learning-portrait\frontend
npm ci
npm run dev
```

打开 `http://localhost:5173`。API 文档位于 `http://localhost:8000/docs`。

### 2.2 演示账号

- 邮箱：`demo@gongxue.local`
- 密码：`demo12345`

演示账号只用于本地展示。用户名可以在“系统设置”修改，顶部会同步显示真实用户名；当前本地演示数据已显示为“张同学”。

### 2.3 Docker 部署

复制 `.env.example` 为 `.env`，至少设置：

```dotenv
DB_PASSWORD=数据库强随机密码
JWT_SECRET=至少32位强随机字符串
```

需要“后端加密保存 API Key”时额外配置：

```dotenv
BYOK_MASTER_KEY=独立强随机主密钥
```

然后执行：

```bash
docker compose up -d --build
```

后端容器启动时会自动运行 Alembic 迁移。停止但保留数据使用 `docker compose down`；不要在正式环境随意加 `-v`。

## 三、学习者完整操作 SOP

推荐按以下顺序操作，才能看到完整的因果闭环。

### 3.1 登录与首页

1. 使用账号登录。
2. 首页查看综合能力、当前路线、资源数量、计划进度、测试趋势和最近学习活动。
3. 点击指标卡或图表节点可打开局部放大详情；弹层不会占满屏幕，关闭后保持原页面上下文。

若未登录访问受保护页面，系统会跳转登录页，并在登录后返回原目标页面。

### 3.2 建立能力画像

1. 进入“能力画像”。
2. 填写背景、目标、偏好、每周学习时间、学习风格、自评和诊断结果。
3. 点击“分析并保存画像”。
4. 查看六维能力雷达、五态学情、优势、盲区和综合分。
5. 点击盲区查看产生原因与建议动作。

画像不是静态问卷结果。后续测试、项目证据和难度反馈都会创建新版本，趋势页可追溯变化原因。

### 3.3 比较并选择路线

1. 进入“方向探索”。
2. 在三个方向簇间切换，浏览 16 个主方向；也可直接搜索岗位或具体技术。
3. 勾选感兴趣的主方向，点击“智能比较主方向”。
4. 比较匹配分、预计周期、技能差距、就业岗位、市场薪资参考、学历要求、匹配理由和反事实结果。
5. 打开主方向详情，逐阶段查看 29 条细分路线的具体技术栈，例如 Vue 路线会明确列出 HTML5、CSS3、JavaScript、TypeScript、Axios、Vue Router、Pinia、测试、性能优化、Nginx、Docker 和 CI/CD。
6. 点击“加入组合路线”，最多选择 6 条；可只生成单路线，也可生成跨方向组合路线。

反事实结果会回答：如果每周多投入 4 小时、补齐关键前置技能，周期或匹配度会如何变化。这避免只给一个不可解释的推荐分。

如已有意向招聘信息，点击“粘贴真实 JD”：

1. 粘贴完整岗位职责与任职要求，可补充原始招聘链接。
2. 检查提取的岗位、城市（支持苏州等任意城市文本）、学历、经验、薪资原文、职责与技术栈。
3. 删除误识别技能并补充缺失字段；未写明的信息保持“未明确”。
4. 确认目标后查看岗位技能覆盖率和优先差距任务。
5. 进入学习中心比较路线 vN 与建议 vN+1；确认前现有路线不变。

### 3.4 查看技能图谱

1. 进入“技能图谱”。
2. 切换路线。
3. 拖动、缩放图谱，查看技能前置关系。
4. 点击节点查看定义、难度、依赖、学习证据和来源。

图谱数据来自版本化计算机领域目录，不是前端写死的装饰图。

### 3.5 检索知识与贡献资料

1. 进入“知识检索”。
2. 输入技能、框架或问题，必要时筛选路线。
3. 查看匹配内容、来源、版本、可信度和检索依据。
4. 点击结果打开详情。
5. 有合适资料时点击“贡献知识”，填写标题、正文、原始链接、许可和版本。

用户贡献不会直接进入正式知识库，而是进入待审核队列。治理管理员审核后才可成为高优先级证据。

### 3.6 运用多智能体协作生成学习闭环

1. 进入“智能生成”。
2. 选择 1–6 条细分技术路线，允许跨前端、后端、AI、算法、系统等主方向。
3. 在“组合路线预演”中确认去重后的技术栈、总周数、阶段顺序、学习动作与验收标准。
4. 填写本次目标与聚焦主题。
5. 选择来源策略。
6. 点击“运用多智能体协作，生成组合学习路线”。
7. 等待后端完成学情建模、跨路线检索、双方案生成与仲裁。
8. 查看质量总分、知识覆盖、引用覆盖、同技能证据匹配、画像适配、前置冲突和系统检出的证据不足风险。
9. 进入“学习计划”，勾选具体技术任务；阶段详情会显示需要提交的证据和验收标准。

会话详情包含：

- Agent 轨迹：结构化任务摘要、状态、耗时和证据；
- 最终讲义：按技能与先修顺序组织的个性化内容；
- 项目实操：步骤、交付物、证据要求和通过条件；
- 仲裁与证据：双候选评分、辩论、裁定、来源及生成审计。

系统只保存可核验的结构化轨迹，不展示或存储模型隐藏思维链。AI 增强内容会被拆为原子主张，逐条检查有效引用、证据原文、数字和技术实体；未通过的主张直接拦截。页面明确说明自动风险指标不是人工盲测幻觉率。

### 3.6.1 工作样本能力测评

1. 进入“工作样本能力测评”，选择本次路线生成的测评。
2. 针对每项技能，分别填写实施方案、验收与验证、失败处理、技术取舍；四个栏目不可复制同一段内容。
3. 添加已有成果证据，可选择代码仓库、提交哈希、测试结果、部署地址、截图说明或复盘说明。
4. 提交后查看五项得分、证据可信度、缺失项和具体改进建议。
5. 有合格成果证据且方案得分达到门槛时，对应技能才按证据可信度回写画像；只有文字回答时结果标记为“形成性”，不会抬高画像。

注意：仓库和部署链接当前只校验 URL 格式，系统不会声称已经访问或人工验真。正式能力认证应结合代码沙箱、CI 结果或导师复核。

### 3.7 使用讲义

1. 进入“个性化讲义”。
2. 打开资源详情。
3. 按章节学习，点击引用查看原来源。
4. 查看知识版本、生成时间和来源类型。
5. 从资源页进入关联会话，复核仲裁和审计信息。

### 3.8 完成项目实操

1. 进入“项目实操”。
2. 打开任务并逐步完成。
3. 勾选已完成步骤。
4. 填写或附上运行证据说明，例如测试结果、截图说明、日志摘要、仓库提交标识。
5. 提交评估。

系统按步骤完成度和证据完整度评分，给出是否通过及下一动作。未提供证据不能只靠“我做完了”获得高分。

### 3.9 完成分阶测试

1. 进入“分阶测试”。
2. 打开当前路线测试。
3. 提交每题的方案、边界、验证方法和通过标准。
4. 查看逐题得分、反馈和涉及技能。

测试成绩会生成 Assessment Attempt，并按证据权重回写技能分和画像新版本。

### 3.10 学习计划与打卡

1. 进入“学习中心”，优先查看当前阶段的本周任务，而不是先浏览整张大路线。
2. 打开任务查看学习动作、所需成果和验收标准。
3. 完成后点击“提交成果”，填写仓库链接、Git 提交、测试结果、部署地址或文档，并说明与验收标准的关系。
4. 系统显示证据分数和校验边界；仅 URL 格式正确不等于项目真实可运行，仍需沙箱执行或人工复核。
5. 勾选已完成项，填写本周实际投入和“正常、太难、卡住、太简单、时间不足”反馈后保存。
6. 非正常反馈会生成路线调整建议：过难/卡住时增加前置回补，太简单时压缩重复训练，时间不足时重算阶段周期。
7. 在“路线版本”中比较调整原因、阶段数、总周数和具体变更，再选择接受或拒绝；已接受版本可撤销。
8. 只有测试或通过阈值的成果证据可以更新能力画像，普通勾选不会虚增能力分。

### 3.11 成长报告、记录和消息

- “成长报告”展示能力、盲区、路线依据、测试趋势、质量证据和下一行动，可打开打印版并另存为 PDF。
- “学习记录”聚合资源生成与测试活动。
- “消息中心”接收生成完成、反馈调路等业务通知；点击消息会标记已读并显示关联对象，可前往处理。

### 3.12 系统设置与隐私

在“系统设置”可以：

- 修改用户名和头像；
- 修改密码，并使其他 Refresh Token 失效；
- 导出个人数据；
- 永久删除账号及关联学习数据。

账号删除不可撤销，必须再次输入当前密码确认。

## 四、BYOK、联网检索与来源策略 SOP

### 4.1 新增服务

1. 进入“系统设置”，点击“立即配置外部 API”；也可以直接进入左侧“AI 与搜索”。
2. 点击“新增服务”。
3. 选择 LLM 或联网搜索。
4. 选择 DeepSeek、OpenAI-compatible、Tavily、Serper 或自定义协议。
5. 填写名称、HTTPS 服务地址、模型名称（LLM 必填）和 API Key。
6. 选择保存方式。
7. 设置单次最大 Token、每日请求上限、每日预算、超时和可选单价。
8. 点击“保存并测试”；测试成功后，该服务会出现在“智能生成”的来源配置选择器中。

配置页支持随时启停服务、查看最近连接结果、更换或清除临时密钥。停用、超限或连接失败的服务不会阻塞本地学习功能。

### 4.2 两种密钥模式

- 临时使用：密钥只在后端进程内存中保存，过期、退出或重启后清除，需要重新装载。
- 后端加密保存：数据库只保存密文，前端永远只看到 `••••••••abcd` 形式的尾号。

密钥不会写入浏览器 localStorage、应用日志、Agent 轨迹、学习报告或数据导出。

### 4.3 四种来源策略

| 模式 | 实际行为 | 适用场景 |
|---|---|---|
| 仅知识库 | 只用本地审核证据 | 核心概念、无外部成本、比赛稳定演示 |
| 知识库 + 全网检索 | 后端检索最新资料，不调用生成模型 | 框架版本、技术更新 |
| 知识库 + AI 创作 | 模型只能依据本地证据整合 | 个性化讲义、例题、任务改写 |
| 全能力模式 | 本地、检索、模型增强、双候选、仲裁、引用校验 | 需要最新性和个性化的完整任务 |

### 4.4 降级规则

以下情况不会让核心学习功能瘫痪：

- 未选择模型或搜索服务；
- 临时密钥过期；
- 连接失败或超时；
- 超过每日请求/预算；
- 模型返回非预期结构；
- 搜索结果缺少合法来源链接。

系统会回退到本地审核知识库和规则生成，并在 `source_audit.fallbacks` 中记录原因。请求策略与实际策略同时展示，不能把降级结果伪装成完整联网结果。

## 五、治理管理员 SOP

治理账号只能由部署者通过环境变量初始化，没有默认弱口令。

管理员可：

1. 查看领域目录版本、路线数、冻结画像数和任务数。
2. 审核知识贡献，填写审核意见。
3. 运行离线可复现的冻结评测。
4. 查看工学智链方法的实际结果。

直接 LLM、单 RAG 等基线只有在使用相同提供方、相同任务与相同评分规则实际运行后才能填入；系统不会伪造更优对比数字。

## 六、比赛核心痛点映射

| 痛点 | 系统机制 | 可现场核验的证据 |
|---|---|---|
| 计算机方向多、学生迷茫 | 16 个主方向、29 条细分路线、画像匹配、方向量表与反事实比较 | 方向探索中的六维匹配、具体技术栈、岗位、差距和周期变化 |
| 方向推荐脱离真实就业目标 | 真实 JD 解析、字段人工确认、岗位技能差距 | 目标岗位页的原文命中、未明确字段、优先差距与待确认路线版本 |
| 学习路线碎片化、先修混乱 | 技能依赖图谱、严谨候选、前置冲突检查 | 图谱箭头、讲义顺序、质量指标中的前置冲突 |
| 通用内容不适合个人 | 五态学情、六维画像、路线与资源联合生成 | 画像版本、`profile_fit`、个性化讲义 |
| 内容可能幻觉、来源不清 | 本地审核优先、段落引用、版本、仲裁和来源审计 | 会话“仲裁与证据”、引用覆盖率、来源详情 |
| 学完不会做、无法证明能力 | 本周任务、验收标准、仓库/提交/测试/部署/文档证据与校验边界 | 成果证据页、证据分数、画像更新前后值 |
| 一次推荐后不再变化 | JD/难度/阻塞/时间/证据触发版本化重排 | 路线版本差异、接受/拒绝/撤销记录 |
| 多 Agent 只是角色扮演 | 双候选独立策略、触发辩论、仲裁融合 | 候选评分、辩论轮次、证据 ID、裁定摘要 |
| 模型成本和厂商锁定 | BYOK、统一兼容网关、预算、限额、降级 | AI 与搜索页、用量统计、实际策略 |
| 技术变化快 | 后端可控联网检索、检索时间和内容版本 | 来源审计中的检索时间、链接、版本 |
| 评测数字不可复现 | 冻结任务、冻结画像、版本化评分规则 | 管理端评测摘要与运行结果 |
| 隐私与密钥风险 | 用户隔离、HttpOnly 刷新、密钥脱敏、导出不含明文 | 服务配置返回、数据导出、账号注销 |

## 七、8 分钟演示脚本

1. 0:00–0:40：登录，指出首页所有数据均来自业务数据库。
2. 0:40–1:30：建立/展示画像，解释五态与六维，而非单一分数。
3. 1:30–2:30：比较 Agent 全栈、后端和前端，展示反事实周期和技能差距。
4. 2:30–3:00：打开技能图谱，展示先修关系。
5. 3:00–4:30：用“仅知识库”配置启动多智能体协作，展示完整协作轨迹和质量指标。
6. 4:30–5:30：打开仲裁与证据，展示双候选、辩论、引用和来源版本。
7. 5:30–6:20：提交“太难”，展示即时调路通知；打开消息详情。
8. 6:20–7:10：展示项目证据评分、测试回写画像和计划打卡。
9. 7:10–8:00：打开“AI 与搜索”，说明 BYOK、预算、四策略和安全降级。

演示时优先使用“仅知识库”，保证无网络、无第三方余额时仍可稳定完成。随后再展示外部服务设置，避免把比赛现场网络当成核心依赖。

## 八、测试与验收记录

2026-07-24 已完成：

- 后端 Ruff 静态检查：通过，零问题。
- 后端 Pytest：8 项全部通过。
- Alembic：空 SQLite 数据库可从零升级到 `001_current_schema`。
- 前端：`vue-tsc` 通过，Vite 生产构建成功。
- 浏览器真实交互：登录保护、张同学动态用户名、画像持久化、三路线对比、路线选择、多智能体协作生成、100% 知识/引用覆盖、会话轨迹、仲裁证据、反馈调路、消息已读、BYOK 表单均通过。
- 详情动效：1280×720 视口中弹层约 820×330，保留页面上下文，不占满屏幕。
- 修复后浏览器流程未新增运行错误。
- BYOK 集成测试覆盖：密钥脱敏、用户隔离、全能力模拟调用、用量记录和外部失败降级。

执行命令：

```powershell
cd E:\multi-agent-engi-learning-portrait\backend
.\venv\Scripts\python.exe -m ruff check app tests
.\venv\Scripts\python.exe -m pytest
$env:DATABASE_URL='sqlite:///:memory:'
.\venv\Scripts\python.exe -m alembic upgrade head

cd E:\multi-agent-engi-learning-portrait\frontend
npm run build
```

Pytest 当前有一条来自 Starlette TestClient 依赖层的弃用提示，不影响测试通过和业务运行。当前机器未安装 Docker，因此已验证 Dockerfile、Compose 配置、生产入口和迁移链，但没有在本机执行容器启动；部署机需按上线检查清单再跑一次 `docker compose up -d --build`。

## 九、常见故障

### 9.1 页面跳回登录

Access Token 已失效且 Refresh Cookie 不可用。重新登录即可。检查前后端域名、CORS 和 Cookie Secure 设置是否匹配。

### 9.2 没有讲义、实操或测试

先选择路线，再在“智能生成”完成一次会话。每个完成会话会持久化生成讲义、实操、测试和计划资源。

### 9.3 图表显示“暂无图表数据”

这是空画像或空测试历史的正常状态。先完成画像或测试。图表异常不会阻断表单和其它业务。

### 9.4 外部服务连接失败

依次检查 HTTPS 地址、模型名称、API Key、余额、每日限额、预算和超时。自定义地址默认不能指向私网或环回地址。核心生成可切回“仅知识库”继续使用。

### 9.5 加密保存不可用

生产环境必须设置独立 `BYOK_MASTER_KEY`。设置后重启服务，再重新输入 API Key；旧主密钥丢失后，已有密文无法恢复。

### 9.6 修改领域目录后没有变化

重启后端以重新加载目录。修改 `catalog.json` 时必须保持路线代码唯一、技能代码唯一、依赖无环，并同步更新冻结评测版本。

## 十、交付文件入口

- 项目说明：`README.md`
- 系统架构：`docs/architecture.md`
- API 说明：`docs/api.md`
- 部署说明：`docs/deployment.md`
- 计算机领域目录：`data/computer_domain/catalog.json`
- 冻结评测：`data/evaluation/`
- 后端应用：`backend/app/`
- 前端应用：`frontend/src/`

任何正式演示前，按本 SOP 的“启动—完整学习闭环—测试与验收”顺序快速复核一次。
