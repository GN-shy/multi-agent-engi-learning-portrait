# 功能测试报告修复回归（2026-08-04）

依据《工学智链（功能测试报告）》中的“问题清单”和高、中严重度测试项完成本轮修复。

## 已修复问题

| 编号 | 修复结果 | 实现要点 | 自动回归结果 |
|---|---|---|---|
| BUG-01 | 已修复 | 补齐实操反馈查询，在 Records API 中批量关联会话，消除未定义变量与逐条重复查询 | `GET /api/v1/records` 返回 200 |
| BUG-02 | 已修复 | 评测摘要和评测执行统一使用管理员权限闸门 | 学习者访问摘要返回 403 |
| BUG-03 | 已修复 | 历史空 JSON/数值字段提供稳定默认值，新增可读画像解读、优势、短板、可信度与下一行动 | 画像顶层空字段为 0 个 |
| BUG-04 | 已修复 | 知识索引从主技能扩展到 29 条细分路线的全部阶段技术栈，增加短语与部分词匹配 | `Python` 返回 8 条，知识详情返回 200 |
| BUG-05 | 已修复 | 组合器保留正式 `phases`，并兼容旧客户端 `stages` 字段 | Agent 全栈 + Python 后端返回 9 个阶段 |
| BUG-06 | 已修复 | 四种来源策略在前端使用可信中文映射，阻断异常编码文本显示 | 前端生产编译通过 |
| BUG-07 | 已修复 | 分析、测评、反馈继续写入快照；旧账号无快照时以当前画像建立趋势起点 | 演示账号趋势返回 4 条 |
| BUG-08 | 已修复 | 中文用户名进行 Unicode NFKC 规范化并过滤不可见字符，密码要求至少三类字符 | 中文用户名校验通过，弱密码被拒绝 |

## 同步体验优化

- 画像页增加首次使用三步引导和自然语言解读面板。
- 知识检索空状态增加 Python、Vue 3、React、Java、Agent、算法、嵌入式等快捷搜索。
- 健康接口增加数据库连接、细分路线数量、知识索引状态与知识片段数量。
- 注册页增加密码强度进度与明确中文校验提示。
- 知识片段规模由只覆盖主技能扩展为 235 条可检索片段。

## 自动验证

- Python 语法树校验通过。
- Vue / TypeScript 生产编译通过。
- 29 条单路线及两两组合均保证至少一个阶段，不再出现空阶段。
- 资源列表、学习记录、知识详情、画像、趋势、路线组合接口均返回成功。
- 普通学习者访问治理评测接口稳定返回 403，而非 500。

## 后续增强项（不属于本轮阻断缺陷）

- 六 Agent 会话改为异步任务与流式增量返回，降低大响应等待感。
- 补充 WebSocket 长连接、重连与消息格式的端到端自动化测试。
- 为路线详情和对比结果增加字段裁剪、缓存和分享链接。
- 增加报告 PDF 服务端导出、长列表虚拟滚动和首页渐进加载。
- 后续接入可信岗位数据源，定期更新薪资范围与数据日期。
